<!-- include header -->
<%- include('partials/_header')%>
<div class="container">
  <a href="/" class="button"><i class="fas fa-backward"></i></a>
  </div>

<div class="container b d-grid gap-2 d-md-flex justify-content-md-end">
    <a href="/edit/<%= user.id %>" type="submit" class="btn btn-primary">Edit</a>
    <a href="/delete/<%= user.id %>"  class="btn btn-danger " onclick="return confirm('Are you sure you want to delete this item ??')">Delete</a>


   <!-- <a href="/delete/<%= user.id %>" title="delete" class="delete" onclick="return confirm('Are you sure you want to delete this item')">Delete</a> -->
  </div>

<div class="container">
    <div class="container head">
        <h2>Blog Description</h2>
    </div>
    <!-- <h2><%= title %></h2> -->
    <div class="card show" >
        <div class="card-body">
          <h5 class="card-title text-uppercase">Title : <%= user.title %></h5>
          <h6 class="card-subtitle mb-2 text-muted">Categories: <%= user.categories %></h6>
          <p class="card-text"><%= user.content %></p>
        </div>
      </div>
     
  </div>
  <%- include('partials/_footer')%>
